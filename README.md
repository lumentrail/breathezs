# BreathEZs® Website

Marketing site for BreathEZs (BEZ) — premium men's underwear designed to breathe.

- `index.html` — self-contained single-page site (no build step, no dependencies).
- Waitlist forms currently store emails in localStorage; wire up a real email-capture service (Mailchimp, Klaviyo, etc.) before launch.

To preview locally, just open `index.html` in a browser.
